<%= form_with(model: libretum, local: true) do |form| %>
  <% if libretum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(libretum.errors.count, "error") %> prohibited this libretum from being saved:</h2>

      <ul>
      <% libretum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :Nombre %>
  </div>

  <div class="field">
    <%= form.label :Telefono %>
    <%= form.number_field :Telefono %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
